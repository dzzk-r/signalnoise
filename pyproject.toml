[project]
name = "signalnoise"
version = "0.1.0"
description = "Extract structured system knowledge from raw conversations."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
signalnoise = "pipelines.speech.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pipelines"]

[project.optional-dependencies]
dev = []
